# Resume Builder Agent — Frontend

Next.js app. See the [top-level README](../README.md) for full setup
instructions (backend, database, OAuth).

Quick start:

```bash
npm install
cp .env.local.example .env.local
npm run dev
```
