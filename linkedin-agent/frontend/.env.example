# Base URL of the FastAPI backend. Leave unset in local dev -- the Vite
# dev server proxies /api to http://localhost:8000 automatically (see
# vite.config.ts). Set this explicitly for a production build.
VITE_API_BASE_URL=
